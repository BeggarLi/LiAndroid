<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.liandroid"
  xmlns:android="http://schemas.android.com/apk/res/android">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.LiAndroid">
    <activity android:name=".CustomTest.PopupWindow.PopupWindowActivity"></activity>
    <activity android:name=".CustomTest.ViewpageActivity" />
    <activity android:name=".CustomTest.MenuTestActivity"/>
    <activity android:name=".CustomTest.CustomViewActivity" />
    <activity android:name=".event.KeyEventTestActivity" />
    <activity android:name=".event.MotionEventTestActivity" />
    <activity android:name=".event.EventTestActivity" />
    <activity android:name=".listview.ListViewActivity" />
    <activity android:name=".recyclerview.RecyclerViewActivity" />
    <activity android:name=".runtimepermission.RuntimePermissionActivity" />
    <activity android:name=".handler.HandlerTestActivity" />
    <activity
      android:name=".main.MainActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

  </application>

  <uses-permission android:name="android.permission.CALL_PHONE" />


</manifest>